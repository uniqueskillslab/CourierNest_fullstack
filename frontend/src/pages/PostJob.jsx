import React, { useState } from 'react';
import { api } from '../api';
import { useNavigate } from 'react-router-dom';
export default function PostJob({ user }) { const nav = useNavigate(); const [form,setForm]=useState({ title:'', company:'', location:'', job_type:'Internship', pay_type:'Unpaid', level:'All', description:'' }); function submit(e){ e.preventDefault(); api.postJob(form).then(()=>nav('/browse')).catch(()=>alert('Error posting')) } return (<div className='card'><h3>Post a Job / Internship</h3><form onSubmit={submit} className='small'><input required placeholder='Job title' value={form.title} onChange={e=>setForm({...form,title:e.target.value})} /><input required placeholder='Company' value={form.company} onChange={e=>setForm({...form,company:e.target.value})} /><input placeholder='Location' value={form.location} onChange={e=>setForm({...form,location:e.target.value})} /><select value={form.job_type} onChange={e=>setForm({...form,job_type:e.target.value})}><option>Internship</option><option>Job</option></select><select value={form.pay_type} onChange={e=>setForm({...form,pay_type:e.target.value})}><option>Paid</option><option>Unpaid</option></select><select value={form.level} onChange={e=>setForm({...form,level:e.target.value})}><option>All</option><option>Fresher</option><option>Experienced</option></select><textarea rows={5} placeholder='Description' value={form.description} onChange={e=>setForm({...form,description:e.target.value})} /><button className='btn btn-primary'>Post</button></form></div>); }
